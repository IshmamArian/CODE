<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WEBSITE HACKED BY ARIAN</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=VT323&display=swap');

    body {
      margin: 0;
      padding: 0;
      font-family: 'VT323', monospace;
      background: #000;
      color: #00ff00;
      text-align: center;
      overflow: hidden;
      position: relative;
    }

    #matrix-canvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -2;
      opacity: 0.6;
    }

    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: repeating-linear-gradient(
        to bottom,
        transparent,
        rgba(0, 255, 0, 0.1) 1px,
        transparent 2px
      );
      animation: scan 2s linear infinite;
      z-index: -1;
    }

    @keyframes scan {
      0% { background-position: 0 -100%; }
      100% { background-position: 0 100%; }
    }

    .container {
      position: relative;
      z-index: 1;
      padding: 50px;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    h1 {
      font-size: 48px;
      text-shadow: 0 0 15px #00ff00, 0 0 30px #ff0000;
      animation: glitch 1s infinite alternate;
    }

    @keyframes glitch {
      0% { transform: skew(0deg); text-shadow: 2px 2px #ff0000, -2px -2px #00ffff; }
      20% { transform: skew(5deg); text-shadow: -2px 2px #00ff00, 2px -2px #ff00ff; }
      40% { transform: skew(-5deg); text-shadow: 2px -2px #00ffff, -2px 2px #ff0000; }
      60% { transform: skew(10deg); text-shadow: -2px -2px #ff00ff, 2px 2px #00ff00; }
      80% { transform: skew(-10deg); text-shadow: 2px 2px #ff0000, -2px -2px #00ffff; }
      100% { transform: skew(0deg); text-shadow: 2px 2px #ff0000, -2px -2px #00ffff; }
    }

    p {
      font-size: 24px;
      margin: 20px 0;
      animation: fade-in 2s ease-in-out;
    }

    @keyframes fade-in {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }

    .contact {
      margin-top: 50px;
      font-size: 20px;
      color: #ff00ff;
      text-shadow: 0 0 10px #ff00ff;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    @media (max-width: 768px) {
      h1 { font-size: 32px; }
      p { font-size: 18px; }
      .container { padding: 20px; }
    }
  </style>
</head>
<body>
  <canvas id="matrix-canvas"></canvas>
  <div class="container">
    <h1>WEBSITE HACKED BY ARIAN</h1>
    <p>Your system has been compromised.</p>
    <div class="contact">Contact for recovery: <a href="mailto:coded.by.arian@gmail.com" style="color: #ff00ff; text-decoration: none;">coded.by.arian@gmail.com</a></div>
  </div>

  <script>
    // Matrix Rain Animation
    const canvas = document.getElementById('matrix-canvas');
    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789@#$%^&*()_+-=[]{}|;:,.<>?';
    const fontSize = 16;
    const columns = Math.floor(canvas.width / fontSize);
    const drops = Array(columns).fill(1);

    function drawMatrix() {
      ctx.fillStyle = 'rgba(0, 0, 0, 0.05)';
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = '#00ff00';
      ctx.font = `${fontSize}px monospace`;

      drops.forEach((y, i) => {
        const text = chars[Math.floor(Math.random() * chars.length)];
        ctx.fillText(text, i * fontSize, y * fontSize);
        drops[i] = y > canvas.height / fontSize || Math.random() > 0.975 ? 0 : y + 1;
      });
    }

    setInterval(drawMatrix, 50);

    window.addEventListener('resize', () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    });

    // Sequential Hacking Alerts
    window.onload = function() {
      const alerts = [
        "Initiating breach protocol...",
        "Firewall bypassed successfully.",
        "Intrusion detection systems disabled.",
        "Admin panel hijacked.",
        "Database access granted.",
        "User credentials compromised.",
        "Encrypting sensitive files...",
        "Ransomware deployed.",
        "Backdoor installed for persistent access.",
        "Network traffic rerouted.",
        "Root privileges obtained.",
        "System logs erased.",
        "Malware injection complete.",
        "Phishing module activated.",
        "DDoS attack vector prepared.",
        "Zero-day exploit executed.",
        "Security certificates revoked.",
        "VPN tunnels infiltrated.",
        "API endpoints hijacked.",
        "Web server configuration altered.",
        "All defenses neutralized. System under control."
      ];

      let index = 0;
      function showNextAlert() {
        if (index < alerts.length) {
          alert(alerts[index]);
          index++;
          setTimeout(showNextAlert, 500); // Delay between alerts for dramatic effect
        }
      }

      showNextAlert();
    };
  </script>
</body>
</html>